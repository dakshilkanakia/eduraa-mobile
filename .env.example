# Eduraa Mobile — Environment Variables
# Copy this to .env and fill in your values

# API URL — use your local IP (not localhost) when testing on a physical device
# For simulator: http://localhost:8000
# For physical device: http://192.168.x.x:8000 (your machine's local IP)
EXPO_PUBLIC_API_URL=http://localhost:8000
